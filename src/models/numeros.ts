class Numeros {
  id: number
  numero: number
  nome: string
  email: string

  constructor(id: number, numero: number, nome: string, email: string) {
    this.id = id
    this.numero = numero
    this.nome = nome
    this.email = email
  }
}

export default Numeros
